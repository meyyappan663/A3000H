# Add project-specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified in
# /usr/local/Cellar/android-sdk/24.3.3/tools/proguard/proguard-android-optimize.txt
# You can edit the include path and order by changing the proguardFiles
# directive in build.gradle.
#
# minifyEnabled is currently false for the release build, so this file
# has no effect yet -- it just needs to exist because build.gradle
# references it.
